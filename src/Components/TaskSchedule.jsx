import React from 'react'

const TaskSchedule = ({classes}) => {
  return (
    <section className={` h-full  bg-p1 shadow-xl rounded-md text-xl p-4 ${classes}`}>
      Task schedule
     </section>
  )
}

export default TaskSchedule